// Magic: adb287b4
// Automatically generated PSI dialog (PSI version 19.0.383)
// Tue Nov 30 17:18:43 2021


f_tmp = FLOAT_FIELD;	// dialog.top.ui
passthrough.gad = NULL { }

aov_input.gad = 
{
    LAYOUT (horizontal) HSTRETCH
    { WIDTH(0.18) }

    aov_input.label = PARM_LABEL("AOV Input", "aov_input") WIDTH(1.5);
    PARM(float, 3, "aov_input", none, COLOR, "R,G,B") 
	LABELREF(aov_input.label) HSTRETCH;

    aov_input.exportbtn = PARM_EXPORTBUTTON "SOP_sphere"
	VALUE(aov_input.export) LOOKFLAT
	HELP("Open Shader Effects menu (Ctrl+LMB to promote this parameter)")
	UI_FLAT_ICON_BTN_SIZE_SMALL;

    { WIDTH(0.1) }
}

aov_name.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    aov_name.label = PARM_LABEL("AOV Name", "aov_name") WIDTH(1.5);
    PARM_FIELD(string, "aov_name") VALUE(aov_name.val) 
	LABELREF(aov_name.label) HSTRETCH;

    aov_name.exportbtn = PARM_EXPORTBUTTON "SOP_sphere"
	VALUE(aov_name.export) LOOKFLAT
	HELP("Open Shader Effects menu (Ctrl+LMB to promote this parameter)")
	UI_FLAT_ICON_BTN_SIZE_SMALL;

    { WIDTH(0.1) }
}

blend_opacity.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("blend_opacity", "Blend Opacity") toggle VALUE(blend_opacity.val);

    { HSTRETCH }

    blend_opacity.exportbtn = PARM_EXPORTBUTTON "SOP_sphere"
	VALUE(blend_opacity.export) LOOKFLAT
	HELP("Open Shader Effects menu (Ctrl+LMB to promote this parameter)")
	UI_FLAT_ICON_BTN_SIZE_SMALL;

    { WIDTH(0.1) }
}
shader.gad = NULL { }

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    passthrough.gad; 
    aov_input.gad; 
    aov_name.gad; 
    blend_opacity.gad; 
    shader.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
