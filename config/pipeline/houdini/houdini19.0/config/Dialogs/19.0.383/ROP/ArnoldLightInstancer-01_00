// Magic: 739fe66b
// Automatically generated PSI dialog (PSI version 19.0.383)
// Fri Dec  3 13:35:01 2021


f_tmp = FLOAT_FIELD;	// dialog.top.ui

sepparm.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}
call_intanceScript_create.row.join =		
{

  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)

call_intanceScript_create.gad = ROW
{
    JUSTIFY(left, center)
    { WIDTH(0.18) }

    PARM_BUTTON("call_intanceScript_create", "Create  /  Update")
	push VALUE(call_intanceScript_create.val);

    {  }
}

call_intanceScript_destroy.gad = ROW
{
    JUSTIFY(left, center)
    {  }

    {  }
    { WIDTH(0.1) }

    PARM_BUTTON("call_intanceScript_destroy", "Destroy")
	push VALUE(call_intanceScript_destroy.val);

    { WIDTH(0.1) }
}
}	// Template join.bot


sepparm2.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

linkPosPts.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("linkPosPts", "Link position to the points") toggle VALUE(linkPosPts.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

sepparm4.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

scatterPath.gad = 
{
 LAYOUT(horizontal) HSTRETCH JUSTIFY(left,center)
 { WIDTH(0.18) }

 scatterPath.label = PARM_LABEL("Point for Intancing", "scatterPath") WIDTH(1.5);
 PARM_FIELD(pathstring, "scatterPath") VALUE(scatterPath.val) 
	LABELREF(scatterPath.label) HSTRETCH;

 ACTION_ICONBUTTON "BUTTONS_jump" VALUE(scatterPath.jump)
    HELP("Jump to operator\nCtrl+LMB: Open parameters popup dialog")
    UI_FLAT_ICON_BTN_SIZE_SMALL LOOKFLAT;

 ACTION_ICONBUTTON "BUTTONS_chooser_node" VALUE(scatterPath.plus)
    HELP("Open floating operator chooser") UI_FLAT_ICON_BTN_SIZE_SMALL 
    LOOKFLAT;

 { WIDTH(0.1) }
}

nameNode.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    nameNode.label = PARM_LABEL("Name of Instance's Node", "nameNode") WIDTH(1.5);
    PARM_FIELD(string, "nameNode") VALUE(nameNode.val) 
	LABELREF(nameNode.label) HSTRETCH;

    { WIDTH(0.1) }
}

instanceName.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    instanceName.label = PARM_LABEL("Name of Instance", "instanceName") WIDTH(1.5);
    PARM_FIELD(string, "instanceName") VALUE(instanceName.val) 
	LABELREF(instanceName.label) HSTRETCH;

    { WIDTH(0.1) }
}

sepparm3.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

revertParmValue.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    revertParmValue.label = PARM_LABEL("Revert Parm Value to Default", "revertParmValue") WIDTH(1.5);
    PARM_FIELD(string, "revertParmValue") VALUE(revertParmValue.val) 
	LABELREF(revertParmValue.label) HSTRETCH;

    { WIDTH(0.1) }
}

sepparm5.gad = 

{
    LAYOUT(vertical) HSTRETCH HMARGIN(0.18, 0.1)
    NO_MARGIN_SPACING_SCALE

    { HEIGHT(0.1) }
    SEPARATOR;
    { HEIGHT(0.1) }
}

linkAttribPt.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    linkAttribPt.label = PARM_LABEL("Link Instance's Parms with Points", "linkAttribPt") WIDTH(1.5);
    PARM_FIELD(string, "linkAttribPt") VALUE(linkAttribPt.val) 
	LABELREF(linkAttribPt.label) HSTRETCH;

    { WIDTH(0.1) }
}

copyPtAttribValue.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    copyPtAttribValue.label = PARM_LABEL("Copy Points' Attributes Value in Parms", "copyPtAttribValue") WIDTH(1.5);
    PARM_FIELD(string, "copyPtAttribValue") VALUE(copyPtAttribValue.val) 
	LABELREF(copyPtAttribValue.label) HSTRETCH;

    { WIDTH(0.1) }
}

linkParmFirstInstance.gad = ROW
{
    HSTRETCH JUSTIFY(left, center)

    { WIDTH(0.18) }

    linkParmFirstInstance.label = PARM_LABEL("Link Instance's Parms with First Instance's Parms", "linkParmFirstInstance") WIDTH(1.5);
    PARM_FIELD(string, "linkParmFirstInstance") VALUE(linkParmFirstInstance.val) 
	LABELREF(linkParmFirstInstance.label) HSTRETCH;

    { WIDTH(0.1) }
}
execute.row.join =		
{

  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
execute.gad = NULL { }
renderdialog.gad = NULL { }
}	// Template join.bot


dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    sepparm.gad; 
    call_intanceScript_create.row.join; 
    sepparm2.gad; 
    linkPosPts.gad; 
    sepparm4.gad; 
    scatterPath.gad; 
    nameNode.gad; 
    instanceName.gad; 
    sepparm3.gad; 
    revertParmValue.gad; 
    sepparm5.gad; 
    linkAttribPt.gad; 
    copyPtAttribValue.gad; 
    linkParmFirstInstance.gad; 
    execute.row.join; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
